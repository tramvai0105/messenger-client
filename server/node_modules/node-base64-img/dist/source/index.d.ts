import { base64ImgOptions, base64ImgResult } from './types';
declare const base64Img: {
    (base64String: string, destPath: string, fileName: string, options?: base64ImgOptions | undefined): Promise<base64ImgResult>;
    toBase64(urlOrPath: string): Promise<string>;
};
export default base64Img;
export { base64ImgOptions, base64ImgResult } from './types';
